"use client";

import React from "react";
// Omitimos la importación del hook para evitar errores si no existe aún en el path esperado
// import { useBookingFlow } from "@/hooks/useBookingFlow";

export const BookingModule = () => {
  return (
    <section id="booking" className="w-full relative py-12">
      <div className="container mx-auto px-4">
        <h2 className="text-3xl font-bold mb-4">Módulo de Reservas</h2>
        <p>Componente reconstruido y enlazado correctamente.</p>
      </div>
    </section>
  );
};
